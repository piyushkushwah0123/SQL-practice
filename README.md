# 📊 SQL Practice & Concepts

This repository contains my learning and practice work in **SQL (Structured Query Language)**.  
It covers basic to intermediate concepts with examples and queries.

---

## 🚀 Topics Covered

- ✅ Basic Queries (SELECT, WHERE, ORDER BY)
- ✅ Filtering Data (AND, OR, NOT, BETWEEN, IN)
- ✅ Aggregate Functions (COUNT, SUM, AVG, MAX, MIN)
- ✅ GROUP BY & HAVING
- ✅ Joins
  - INNER JOIN
  - LEFT JOIN
  - RIGHT JOIN
- ✅ Subqueries
- ✅ Constraints (PRIMARY KEY, FOREIGN KEY, UNIQUE)
- ✅ Table Creation & Data Insertion

---

## 🛠️ Tools Used

- MySQL / PostgreSQL (any SQL-compatible database)
- SQL Workbench / VS Code

---

## 📌 Sample Query

```sql
SELECT name, salary
FROM employees
WHERE salary > 50000
ORDER BY salary DESC;
